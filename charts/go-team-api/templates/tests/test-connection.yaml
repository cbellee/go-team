apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-team-api.fullname" . }}-test-connection"
  labels:
{{ include "go-team-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "go-team-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
